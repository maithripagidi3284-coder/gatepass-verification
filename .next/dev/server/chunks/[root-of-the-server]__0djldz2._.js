module.exports = [
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/net [external] (net, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("net", () => require("net"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/action-async-storage.external.js [external] (next/dist/server/app-render/action-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/action-async-storage.external.js", () => require("next/dist/server/app-render/action-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/runtime-reacts.external.js [external] (next/dist/server/runtime-reacts.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/runtime-reacts.external.js", () => require("next/dist/server/runtime-reacts.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/node:stream [external] (node:stream, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("node:stream", () => require("node:stream"));

module.exports = mod;
}),
"[externals]/os [external] (os, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("os", () => require("os"));

module.exports = mod;
}),
"[externals]/perf_hooks [external] (perf_hooks, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("perf_hooks", () => require("perf_hooks"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/tls [external] (tls, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("tls", () => require("tls"));

module.exports = mod;
}),
"[project]/gatepass/src/app/api/gatepass/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GET",
    ()=>GET,
    "POST",
    ()=>POST
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/gatepass/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/gatepass/src/lib/auth.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/gatepass/src/lib/db.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$date$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/gatepass/src/lib/date.ts [app-route] (ecmascript)");
;
;
;
;
async function GET(req) {
    const session = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getSession"])();
    if (!session) return __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
        error: "Sign in required."
    }, {
        status: 401
    });
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["sweepExpired"])();
    const { searchParams } = new URL(req.url);
    const status = searchParams.get("status") ?? undefined;
    let mentorId = searchParams.get("mentorId") ?? undefined;
    let studentId = searchParams.get("studentId") ?? undefined;
    // Resource-level authorization: students only ever see their own passes,
    // mentors only ever see passes assigned to them — regardless of what the
    // query string says.
    if (session.role === "student") studentId = session.linkedId ?? "__none__";
    if (session.role === "mentor") mentorId = session.linkedId ?? "__none__";
    const passes = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["listGatePasses"])({
        status,
        mentorId,
        studentId
    });
    return __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json(passes);
}
async function POST(req) {
    const session = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getSession"])();
    if (!session || session.role !== "student") {
        return __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Sign in as a student to request a gate pass."
        }, {
            status: 403
        });
    }
    // Look up by userId (from /me), not linkedId — avoids stale-session
    // issues if linkedId wasn't populated at login time.
    const student = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getStudentByUserId"])(session.userId);
    if (!student) return __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
        error: "Student record not found."
    }, {
        status: 404
    });
    const body = await req.json();
    const { reason, date, outTime } = body;
    if (!reason || !date || !outTime) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Missing fields"
        }, {
            status: 400
        });
    }
    if (date !== (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$date$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["todayIST"])()) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Gate passes can only be requested for today's date."
        }, {
            status: 400
        });
    }
    const pass = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createGatePass"])({
        studentId: student.id,
        mentorId: student.mentorId,
        reason,
        date,
        outTime
    });
    if (!pass) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "You already have an active or pending gate pass. Wait for it to complete before requesting another."
        }, {
            status: 409
        });
    }
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["logAuditEvent"])({
        passId: pass.id,
        eventType: "PASS_CREATED",
        actor: student.name,
        details: `Requested exit for "${reason}" on ${date} at ${outTime}.`,
        result: "SUCCESS"
    });
    return __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json(pass, {
        status: 201
    });
}
}),
"[project]/gatepass/src/lib/auth.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "SESSION_COOKIE_NAME",
    ()=>SESSION_COOKIE_NAME,
    "clearSessionCookie",
    ()=>clearSessionCookie,
    "createSessionToken",
    ()=>createSessionToken,
    "getSession",
    ()=>getSession,
    "requireRole",
    ()=>requireRole,
    "setSessionCookie",
    ()=>setSessionCookie,
    "verifySessionToken",
    ()=>verifySessionToken
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$jose$2f$dist$2f$webapi$2f$jwt$2f$sign$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/gatepass/node_modules/jose/dist/webapi/jwt/sign.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$jose$2f$dist$2f$webapi$2f$jwt$2f$verify$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/gatepass/node_modules/jose/dist/webapi/jwt/verify.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/gatepass/node_modules/next/headers.js [app-route] (ecmascript)");
;
;
const encoder = new TextEncoder();
// IMPORTANT: set a real SESSION_SECRET env var in production. This fallback
// only exists so the prototype runs out of the box in dev.
const SECRET = encoder.encode(process.env.SESSION_SECRET || "dev-only-insecure-secret-change-me");
const SESSION_COOKIE_NAME = "gatepass_session";
async function createSessionToken(payload) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$jose$2f$dist$2f$webapi$2f$jwt$2f$sign$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["SignJWT"]({
        ...payload
    }).setProtectedHeader({
        alg: "HS256"
    }).setIssuedAt().setExpirationTime("12h").sign(SECRET);
}
async function verifySessionToken(token) {
    try {
        const { payload } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$jose$2f$dist$2f$webapi$2f$jwt$2f$verify$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jwtVerify"])(token, SECRET);
        return payload;
    } catch  {
        return null;
    }
}
async function setSessionCookie(token) {
    const store = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["cookies"])();
    store.set(SESSION_COOKIE_NAME, token, {
        httpOnly: true,
        secure: ("TURBOPACK compile-time value", "development") === "production",
        sameSite: "lax",
        path: "/",
        maxAge: 60 * 60 * 12
    });
}
async function clearSessionCookie() {
    const store = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["cookies"])();
    store.delete(SESSION_COOKIE_NAME);
}
async function getSession() {
    const store = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["cookies"])();
    const token = store.get(SESSION_COOKIE_NAME)?.value;
    if (!token) return null;
    return verifySessionToken(token);
}
async function requireRole(...roles) {
    const session = await getSession();
    if (!session || !roles.includes(session.role)) return null;
    return session;
}
}),
"[project]/gatepass/src/lib/date.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/** Always compute "today" in IST, regardless of server/DB timezone. */ __turbopack_context__.s([
    "todayIST",
    ()=>todayIST
]);
function todayIST() {
    return new Date().toLocaleDateString("en-CA", {
        timeZone: "Asia/Kolkata"
    }); // YYYY-MM-DD
}
}),
"[project]/gatepass/src/lib/db.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "consumeExit",
    ()=>consumeExit,
    "consumeReturn",
    ()=>consumeReturn,
    "createGatePass",
    ()=>createGatePass,
    "createMentorRecord",
    ()=>createMentorRecord,
    "createStudentRecord",
    ()=>createStudentRecord,
    "createUserAccount",
    ()=>createUserAccount,
    "emailExists",
    ()=>emailExists,
    "findUserByEmail",
    ()=>findUserByEmail,
    "getGatePassById",
    ()=>getGatePassById,
    "getGatePassByToken",
    ()=>getGatePassByToken,
    "getLinkedIdForUser",
    ()=>getLinkedIdForUser,
    "getMentorByUserId",
    ()=>getMentorByUserId,
    "getStudentById",
    ()=>getStudentById,
    "getStudentByRollNo",
    ()=>getStudentByRollNo,
    "getStudentByUserId",
    ()=>getStudentByUserId,
    "getSuspiciousActivity",
    ()=>getSuspiciousActivity,
    "hodApprovePass",
    ()=>hodApprovePass,
    "hodRejectPass",
    ()=>hodRejectPass,
    "listAuditHistory",
    ()=>listAuditHistory,
    "listGatePasses",
    ()=>listGatePasses,
    "listMentors",
    ()=>listMentors,
    "listStudents",
    ()=>listStudents,
    "listUsers",
    ()=>listUsers,
    "logAuditEvent",
    ()=>logAuditEvent,
    "mentorSetStatus",
    ()=>mentorSetStatus,
    "overrideExit",
    ()=>overrideExit,
    "overrideReturn",
    ()=>overrideReturn,
    "sweepExpired",
    ()=>sweepExpired
]);
/**
 * Postgres adapter — fully wired in.
 * Requires DATABASE_URL in .env.local (see schema.sql for table setup).
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$postgres$2f$src$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/gatepass/node_modules/postgres/src/index.js [app-route] (ecmascript)");
;
let sql = null;
function getSql() {
    if (!sql) {
        if (!process.env.DATABASE_URL) {
            throw new Error("DATABASE_URL is not set. Add it to .env.local before using db.ts.");
        }
        sql = (0, __TURBOPACK__imported__module__$5b$project$5d2f$gatepass$2f$node_modules$2f$postgres$2f$src$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"])(process.env.DATABASE_URL, {
            ssl: "require"
        });
    }
    return sql;
}
async function findUserByEmail(email) {
    const db = getSql();
    const rows = await db`SELECT id, name, email, password_hash, role, status FROM users WHERE lower(email) = lower(${email})`;
    if (rows.length === 0) return null;
    const r = rows[0];
    return {
        id: r.id,
        name: r.name,
        email: r.email,
        passwordHash: r.password_hash,
        role: r.role,
        status: r.status
    };
}
async function getLinkedIdForUser(userId, role) {
    const db = getSql();
    if (role === "student") {
        const rows = await db`SELECT id FROM students WHERE user_id = ${userId}`;
        return rows[0]?.id;
    }
    if (role === "mentor") {
        const rows = await db`SELECT id FROM mentors WHERE user_id = ${userId}`;
        return rows[0]?.id;
    }
    return undefined;
}
async function emailExists(email) {
    const db = getSql();
    const rows = await db`SELECT 1 FROM users WHERE lower(email) = lower(${email})`;
    return rows.length > 0;
}
async function listUsers() {
    const db = getSql();
    return db`SELECT id, name, email, role, status FROM users ORDER BY name`;
}
async function createUserAccount(input) {
    const db = getSql();
    const rows = await db`
    INSERT INTO users (name, email, password_hash, role)
    VALUES (${input.name}, ${input.email}, ${input.passwordHash}, ${input.role})
    RETURNING id, name, email, role, status
  `;
    return rows[0];
}
async function getStudentByUserId(userId) {
    const db = getSql();
    const rows = await db`
    SELECT s.id, u.name, s.roll_no, s.photo_url, s.mentor_id, s.department, s.year, m.name AS mentor_name
    FROM students s
    JOIN users u ON u.id = s.user_id
    JOIN mentors m ON m.id = s.mentor_id
    WHERE s.user_id = ${userId}
  `;
    if (rows.length === 0) return null;
    const r = rows[0];
    return {
        id: r.id,
        name: r.name,
        rollNo: r.roll_no,
        photoUrl: r.photo_url,
        mentorId: r.mentor_id,
        branch: r.department,
        year: r.year,
        mentorName: r.mentor_name
    };
}
async function getMentorByUserId(userId) {
    const db = getSql();
    const rows = await db`
    SELECT id, name FROM mentors WHERE user_id = ${userId}
  `;
    return rows[0] ?? null;
}
async function getStudentById(studentId) {
    const db = getSql();
    const rows = await db`
    SELECT s.id, u.name, s.roll_no, s.photo_url, s.mentor_id, s.parent_phone, s.department, s.year
    FROM students s JOIN users u ON u.id = s.user_id
    WHERE s.id = ${studentId}
  `;
    if (rows.length === 0) return null;
    const r = rows[0];
    return {
        id: r.id,
        name: r.name,
        rollNo: r.roll_no,
        photoUrl: r.photo_url,
        mentorId: r.mentor_id,
        parentPhone: r.parent_phone,
        branch: r.department,
        year: r.year
    };
}
async function getStudentByRollNo(rollNo) {
    const db = getSql();
    const rows = await db`
    SELECT s.id, u.name, s.roll_no, s.photo_url, s.mentor_id, s.parent_phone, s.department, s.year
    FROM students s JOIN users u ON u.id = s.user_id
    WHERE s.roll_no = ${rollNo}
  `;
    if (rows.length === 0) return null;
    const r = rows[0];
    return {
        id: r.id,
        name: r.name,
        rollNo: r.roll_no,
        photoUrl: r.photo_url,
        mentorId: r.mentor_id,
        parentPhone: r.parent_phone,
        branch: r.department,
        year: r.year
    };
}
async function listStudents(filters) {
    const db = getSql();
    const rows = await db`
    SELECT s.id, u.name, s.roll_no, s.photo_url, s.department, s.year, s.mentor_id
    FROM students s JOIN users u ON u.id = s.user_id
    WHERE (${filters.year ?? null}::int IS NULL OR s.year = ${filters.year ?? null})
      AND (${filters.branch ?? null}::text IS NULL OR s.department = ${filters.branch ?? null})
      AND (${filters.rollNo ?? null}::text IS NULL OR s.roll_no = ${filters.rollNo ?? null})
      AND (${filters.prefix ?? null}::text IS NULL OR s.roll_no LIKE ${filters.prefix ? filters.prefix + "%" : null})
  `;
    return rows.map((r)=>({
            id: r.id,
            name: r.name,
            rollNo: r.roll_no,
            photoUrl: r.photo_url,
            branch: r.department,
            year: r.year,
            mentorId: r.mentor_id
        }));
}
async function listMentors() {
    const db = getSql();
    const rows = await db`SELECT id, name FROM mentors`;
    return rows;
}
async function createMentorRecord(name, userId) {
    const db = getSql();
    const rows = await db`INSERT INTO mentors (name, user_id) VALUES (${name}, ${userId}) RETURNING id`;
    return rows[0].id;
}
async function createStudentRecord(input) {
    const db = getSql();
    const rows = await db`
    INSERT INTO students (user_id, roll_no, photo_url, department, year, mentor_id, parent_phone)
    VALUES (${input.userId}, ${input.rollNo}, ${input.photoUrl}, ${input.department}, ${input.year}, ${input.mentorId}, ${input.parentPhone})
    RETURNING id
  `;
    return rows[0].id;
}
async function createGatePass(input) {
    const db = getSql();
    try {
        const rows = await db`
      INSERT INTO gate_passes (student_id, mentor_id, purpose, requested_date, requested_out_time, status)
      VALUES (${input.studentId}, ${input.mentorId}, ${input.reason}, ${input.date}, ${input.outTime}, 'PENDING_MENTOR')
      RETURNING id, status, created_at
    `;
        const r = rows[0];
        return {
            id: r.id,
            studentId: input.studentId,
            mentorId: input.mentorId,
            reason: input.reason,
            date: input.date,
            outTime: input.outTime,
            status: r.status,
            createdAt: new Date(r.created_at).getTime()
        };
    } catch (err) {
        if (err && typeof err === "object" && "code" in err && err.code === "23505") return null;
        throw err;
    }
}
async function listGatePasses(filters) {
    const db = getSql();
    const rows = await db`
    SELECT
      gp.id, gp.student_id, gp.mentor_id, gp.purpose AS reason,
      gp.requested_date AS date, gp.requested_out_time AS out_time,
      gp.status, gp.created_at, gp.mentor_action_at, gp.hod_action_at,
      gp.qr_token, gp.valid_from, gp.valid_until, gp.exit_time, gp.return_time,
      su.name AS student_name, s.roll_no, s.photo_url, s.department, s.year, s.parent_phone,
      m.name AS mentor_name
    FROM gate_passes gp
    JOIN students s ON s.id = gp.student_id
    JOIN users su ON su.id = s.user_id
    JOIN mentors m ON m.id = gp.mentor_id
    WHERE (${filters.status ?? null}::text IS NULL OR gp.status = ${filters.status ?? null})
      AND (${filters.mentorId ?? null}::text IS NULL OR gp.mentor_id = ${filters.mentorId ?? null})
      AND (${filters.studentId ?? null}::text IS NULL OR gp.student_id = ${filters.studentId ?? null})
    ORDER BY gp.created_at DESC
  `;
    return rows.map((r)=>({
            id: r.id,
            studentId: r.student_id,
            mentorId: r.mentor_id,
            reason: r.reason,
            date: r.date,
            outTime: r.out_time,
            status: r.status,
            createdAt: new Date(r.created_at).getTime(),
            mentorActionAt: r.mentor_action_at ? new Date(r.mentor_action_at).getTime() : undefined,
            hodActionAt: r.hod_action_at ? new Date(r.hod_action_at).getTime() : undefined,
            qrToken: r.qr_token ?? undefined,
            validFrom: r.valid_from ? new Date(r.valid_from).getTime() : undefined,
            validUntil: r.valid_until ? new Date(r.valid_until).getTime() : undefined,
            exitTime: r.exit_time ? new Date(r.exit_time).getTime() : undefined,
            returnTime: r.return_time ? new Date(r.return_time).getTime() : undefined,
            student: {
                name: r.student_name,
                rollNo: r.roll_no,
                photoUrl: r.photo_url,
                branch: r.department,
                year: r.year,
                parentPhone: r.parent_phone
            },
            mentor: {
                name: r.mentor_name
            }
        }));
}
async function getGatePassById(id) {
    const db = getSql();
    const rows = await db`
    SELECT
      gp.id, gp.student_id, gp.mentor_id, gp.purpose AS reason,
      gp.requested_date AS date, gp.requested_out_time AS out_time,
      gp.status, gp.created_at, gp.mentor_action_at, gp.hod_action_at,
      gp.qr_token, gp.valid_from, gp.valid_until, gp.exit_time, gp.return_time,
      su.name AS student_name, s.roll_no, s.photo_url, s.department, s.year, s.parent_phone,
      m.name AS mentor_name
    FROM gate_passes gp
    JOIN students s ON s.id = gp.student_id
    JOIN users su ON su.id = s.user_id
    JOIN mentors m ON m.id = gp.mentor_id
    WHERE gp.id = ${id}
  `;
    if (rows.length === 0) return null;
    const r = rows[0];
    return {
        id: r.id,
        studentId: r.student_id,
        mentorId: r.mentor_id,
        reason: r.reason,
        date: r.date,
        outTime: r.out_time,
        status: r.status,
        createdAt: new Date(r.created_at).getTime(),
        mentorActionAt: r.mentor_action_at ? new Date(r.mentor_action_at).getTime() : undefined,
        hodActionAt: r.hod_action_at ? new Date(r.hod_action_at).getTime() : undefined,
        qrToken: r.qr_token ?? undefined,
        validFrom: r.valid_from ? new Date(r.valid_from).getTime() : undefined,
        validUntil: r.valid_until ? new Date(r.valid_until).getTime() : undefined,
        exitTime: r.exit_time ? new Date(r.exit_time).getTime() : undefined,
        returnTime: r.return_time ? new Date(r.return_time).getTime() : undefined,
        student: {
            name: r.student_name,
            rollNo: r.roll_no,
            photoUrl: r.photo_url,
            branch: r.department,
            year: r.year,
            parentPhone: r.parent_phone
        },
        mentor: {
            name: r.mentor_name
        }
    };
}
async function getGatePassByToken(token) {
    const db = getSql();
    const rows = await db`
    SELECT
      gp.id, gp.status, gp.purpose AS reason, gp.requested_date AS date, gp.requested_out_time AS out_time,
      gp.qr_token, gp.valid_until, gp.exit_time, gp.return_time,
      su.name AS student_name, s.roll_no, s.photo_url, s.department,
      m.name AS mentor_name
    FROM gate_passes gp
    JOIN students s ON s.id = gp.student_id
    JOIN users su ON su.id = s.user_id
    JOIN mentors m ON m.id = gp.mentor_id
    WHERE gp.qr_token = ${token}
  `;
    if (rows.length === 0) return null;
    const r = rows[0];
    return {
        id: r.id,
        status: r.status,
        reason: r.reason,
        date: r.date,
        outTime: r.out_time,
        validUntil: r.valid_until ? new Date(r.valid_until).getTime() : undefined,
        exitTime: r.exit_time ? new Date(r.exit_time).getTime() : undefined,
        returnTime: r.return_time ? new Date(r.return_time).getTime() : undefined,
        student: {
            name: r.student_name,
            rollNo: r.roll_no,
            photoUrl: r.photo_url,
            branch: r.department
        },
        mentor: {
            name: r.mentor_name
        }
    };
}
async function mentorSetStatus(id, mentorId, newStatus) {
    const db = getSql();
    const rows = await db`
    UPDATE gate_passes
    SET status = ${newStatus}, mentor_action_at = now()
    WHERE id = ${id} AND mentor_id = ${mentorId} AND status = 'PENDING_MENTOR'
    RETURNING id
  `;
    return rows.length > 0;
}
async function hodApprovePass(id) {
    const db = getSql();
    const token = crypto.randomUUID().replace(/-/g, "");
    const rows = await db`
    UPDATE gate_passes
    SET status = 'APPROVED',
        hod_action_at = now(),
        qr_token = ${token},
        valid_from = now(),
        valid_until = (requested_date::text || ' 16:00:00')::timestamp AT TIME ZONE 'Asia/Kolkata'
    WHERE id = ${id} AND status = 'MENTOR_APPROVED'
    RETURNING valid_until
  `;
    if (rows.length === 0) return null;
    return {
        token,
        validUntil: new Date(rows[0].valid_until).getTime()
    };
}
async function hodRejectPass(id) {
    const db = getSql();
    const rows = await db`
    UPDATE gate_passes SET status = 'HOD_REJECTED', hod_action_at = now()
    WHERE id = ${id} AND status = 'MENTOR_APPROVED'
    RETURNING id
  `;
    return rows.length > 0;
}
async function consumeExit(token) {
    const db = getSql();
    const rows = await db`
    UPDATE gate_passes SET status = 'EXITED', exit_time = now()
    WHERE qr_token = ${token} AND status = 'APPROVED'
    RETURNING id
  `;
    return rows.length > 0;
}
async function consumeReturn(token) {
    const db = getSql();
    const rows = await db`
    UPDATE gate_passes SET status = 'RETURNED', return_time = now()
    WHERE qr_token = ${token} AND status = 'EXITED'
    RETURNING id
  `;
    return rows.length > 0;
}
async function overrideExit(rollNo) {
    const db = getSql();
    const rows = await db`
    UPDATE gate_passes gp
    SET status = 'EXITED', exit_time = now()
    FROM students s
    WHERE gp.student_id = s.id AND s.roll_no = ${rollNo} AND gp.status = 'APPROVED'
    RETURNING gp.id
  `;
    return rows[0]?.id;
}
async function overrideReturn(rollNo) {
    const db = getSql();
    const rows = await db`
    UPDATE gate_passes gp
    SET status = 'RETURNED', return_time = now()
    FROM students s
    WHERE gp.student_id = s.id AND s.roll_no = ${rollNo} AND gp.status = 'EXITED'
    RETURNING gp.id
  `;
    return rows[0]?.id;
}
async function sweepExpired() {
    const db = getSql();
    const expired = await db`
    UPDATE gate_passes SET status = 'EXPIRED'
    WHERE status = 'APPROVED' AND valid_until < now()
    RETURNING id
  `;
    for (const row of expired){
        await logAuditEvent({
            passId: row.id,
            eventType: "PASS_EXPIRED",
            actor: "System",
            details: "Pass validity window elapsed before use.",
            result: "SUCCESS"
        });
    }
}
async function logAuditEvent(event) {
    const db = getSql();
    await db`
    INSERT INTO verification_logs (pass_id, event_type, actor_label, details, result)
    VALUES (${event.passId}, ${event.eventType}, ${event.actor}, ${event.details}, ${event.result})
  `;
}
async function listAuditHistory(limit = 50) {
    const db = getSql();
    const rows = await db`
    SELECT vl.id, vl.pass_id, vl.event_type, vl.details, vl.actor_label AS actor, vl.timestamp, vl.result,
           su.name AS student_name, s.roll_no
    FROM verification_logs vl
    LEFT JOIN gate_passes gp ON gp.id = vl.pass_id
    LEFT JOIN students s ON s.id = gp.student_id
    LEFT JOIN users su ON su.id = s.user_id
    ORDER BY vl.timestamp DESC
    LIMIT ${limit}
  `;
    return rows.map((r)=>({
            id: r.id,
            passId: r.pass_id,
            eventType: r.event_type,
            details: r.details,
            actor: r.actor,
            timestamp: new Date(r.timestamp).getTime(),
            result: r.result,
            studentName: r.student_name ?? undefined,
            rollNo: r.roll_no ?? undefined
        }));
}
async function getSuspiciousActivity() {
    const db = getSql();
    const rows = await db`
    SELECT s.id AS student_id, su.name, s.roll_no,
      COUNT(*) FILTER (WHERE vl.event_type = 'REUSE_ATTEMPT') AS reuse_attempts,
      COUNT(*) FILTER (WHERE vl.result = 'DENIED') AS denied_events,
      COUNT(*) FILTER (WHERE vl.event_type = 'PASS_EXPIRED') AS expired_passes,
      COUNT(*) FILTER (WHERE vl.event_type = 'MANUAL_OVERRIDE') AS overrides
    FROM verification_logs vl
    JOIN gate_passes gp ON gp.id = vl.pass_id
    JOIN students s ON s.id = gp.student_id
    JOIN users su ON su.id = s.user_id
    WHERE vl.timestamp > now() - interval '24 hours'
    GROUP BY s.id, su.name, s.roll_no
    HAVING COUNT(*) FILTER (WHERE vl.event_type = 'REUSE_ATTEMPT') > 0
        OR COUNT(*) FILTER (WHERE vl.result = 'DENIED') > 1
        OR COUNT(*) FILTER (WHERE vl.event_type = 'MANUAL_OVERRIDE') > 0
    ORDER BY reuse_attempts + denied_events DESC
  `;
    return rows.map((r)=>({
            studentId: r.student_id,
            name: r.name,
            rollNo: r.roll_no,
            reuseAttempts: Number(r.reuse_attempts),
            deniedEvents: Number(r.denied_events),
            expiredPasses: Number(r.expired_passes),
            overrides: Number(r.overrides)
        }));
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__0djldz2._.js.map